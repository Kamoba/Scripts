This will create .desktop and icon for any .AppImage stored inside ~/Applications/.

So every time you get new AppImage, run the script again.

```
chmod +x AppImageLauncher.sh
./AppImageLauncher.sh
```
